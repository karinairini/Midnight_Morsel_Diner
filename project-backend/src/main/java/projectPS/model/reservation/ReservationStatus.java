package projectPS.model.reservation;

/**
 * Enumeration representing different statuses of reservations.
 * This enum defines statuses such as RECEIVED, APPROVED, and DECLINED.
 * It is used to classify reservations based on their current status.
 */
public enum ReservationStatus {
    RECEIVED,
    APPROVED,
    DECLINED
}
